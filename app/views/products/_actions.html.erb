<%= link_to 'Show', product_path(product), :remote => true, :class => 'btn btn-success btn-margin-sm' %>
<%= link_to 'Edit', edit_product_path(product), :remote => true, :class => 'btn btn-success btn-margin-sm' %>
<%= link_to 'Delete', product_path(product), :remote => true, :method => :delete, :class => 'btn btn-danger btn-margin-sm', :data =>{ :confirm => 'Are you sure?' } %>
